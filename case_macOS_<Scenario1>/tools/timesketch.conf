# Core DB / broker
SQLALCHEMY_DATABASE_URI = "postgresql://timesketch:timesketch@timesketch-postgres:5432/timesketch"
SECRET_KEY = "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
CELERY_BROKER_URL = "redis://timesketch-redis:6379/0"
CELERY_RESULT_BACKEND = "redis://timesketch-redis:6379/0"

# OpenSearch
OPENSEARCH_HOST = "opensearch"
OPENSEARCH_PORT = 9200

# Required on some recent Timesketch builds (prevents KeyError)
UPLOAD_ENABLED = True
UPLOAD_FOLDER = "/cases/uploads"
